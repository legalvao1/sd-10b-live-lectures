// MÉDIA
function media (num1, num2, num3) {
  return (num1 + num2 + num3) / 3;
}

console.log(media(10, 20, 30));























// ----------------------------------------------------

// NOME COMPLETO
function fullName (nome, sobrenome) {
  return (nome + ' ' + sobrenome);
}

// console.log(fullName('Gustavo', 'Caetano'));























// ----------------------------------------------------

// ARRAY COM 100 CARACTERES IGUAIS






























// ----------------------------------------------------

//MEGA-SENA <3
let firstNumber = randomNumber();
let secondNumber = randomNumber();
let thirdNumber = randomNumber();
let fourthNumber = randomNumber();
let fifthNumber = randomNumber();
let sixthNumber = randomNumber();

function randomNumber () {
  return Math.ceil(Math.random() * 60);
}

// console.log(firstNumber, secondNumber, thirdNumber, fourthNumber, fifthNumber, sixthNumber);
